Context from PR 24 discussion_r3170058741: the review on apps/stock/models.py flagged possible inconsistency among `requisicao`, `item_requisicao`, and `material` in `MovimentacaoEstoque`. Decision taken in the PR was to document the invariant in the model, enforce it through `clean()/full_clean()` plus regression tests at the ORM level, and leave a database trigger/migration as an explicit follow-up if durable DB-level enforcement is required later. The immediate rule recorded is that `SALDO_INICIAL` never references requisicao/item, and `RESERVA_POR_AUTORIZACAO` must reference the requisicao/item and keep material aligned with the item.